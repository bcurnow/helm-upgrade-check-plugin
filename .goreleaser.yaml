version: 2

project_name: helm-update-plugin

release:
  github:
    owner: bcurnow
    name: helm-update-plugin
  draft: false
  prerelease: "false"

builds:
  - id: helm-update-plugin
    main: ./cmd/helm-update-check
    binary: helm-update-check
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.Version={{.Version}}
    mod_timestamp: '{{ .CommitTimestamp }}'

archives:
  - id: helm-update-plugin-archive
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    files:
      - README.md
      - LICENSE
      - INSTALL.md
      - CHANGELOG.md
      - plugin.yaml

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

snapshot:
  name_template: '{{ incpatch .Version }}-next'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - Merge pull request
      - Merge branch

env_files:
  github_token: ~/.config/goreleaser/helm-update-plugin-github-token
